import { UshaGanduPipe } from './usha-gandu.pipe';

describe('UshaGanduPipe', () => {
  it('create an instance', () => {
    const pipe = new UshaGanduPipe();
    expect(pipe).toBeTruthy();
  });
});
